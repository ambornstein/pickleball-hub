type Venue = {
    _id: string
    name: string,
    address: string,
    phoneNumber: string,
    url: string,
    zipcode: number,
    openPlay: boolean,
    reservations: boolean,
    lessons: boolean
}